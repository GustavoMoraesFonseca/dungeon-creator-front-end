import { environment } from '../../../environments/environment';

export class UrlConstants {
  static MONSTER_URL      = `${environment.url}/monstros`;
  static MONSTER_TYPE_URL = `${environment.url}/monster-type`;
  static BIOME_URL        = `${environment.url}/biome`;
  static BOOK_URL         = `${environment.url}/book`;
}
